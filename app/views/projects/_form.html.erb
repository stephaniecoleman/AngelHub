<div class="well" role="form">
<%= form_for(:project, url: projects_path, :html => {:class => "form-horizontal"}) do |f| %>

<div class="control-group">
	<%= f.label :desired_project, :class => 'control-label' %>
	<div class="controls">
  <%= f.text_field :title, :class => "text_field", placeholder: "Project name here", :maxlength => 30 %>
  </div>
 </div>

 <div class="control-group">
	<%= f.label :description, :class => 'control-label' %>
	<div class="controls">
  <%= f.text_area :description, :class => "text_field", placeholder: 'What do you envision for this project?' %>
  </div>
 </div>

 <%= f.hidden_field :status, :value => :requested  %>
<br>
<div class="form-actions">
  <%= f.submit :class => 'btn btn-info' %>
</div>
<% end %>
</div>